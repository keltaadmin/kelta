/**
 * ============================================================
 * KELTA Enterprise Platform
 * Platform QR Infrastructure
 * ============================================================
 */

export const QR_DEFAULTS = Object.freeze({
  WIDTH: 300,
  MARGIN: 2,
  ERROR_CORRECTION_LEVEL: 'M',
  TYPE: 'image/png',
});

export const QR_PAYLOAD_VERSION = 1;

export const QR_ISSUER = 'KELTA';
